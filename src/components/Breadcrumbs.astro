---
import { CRoutes } from "@/models"
import { getBreadcrumbs } from "@/utils"
const breadcrumbs = getBreadcrumbs(Astro.url);
---

<div class="breadcrumbs">
  {
    Astro.url.pathname === CRoutes.home.path
    ?
    <span class="breadcrumbs__first" transition:name="breadcrumb__firstPart" >{breadcrumbs.firstPartOfThePath}</span>
    :
    <a href={CRoutes.home.path} class="breadcrumbs__first" transition:name="breadcrumb__firstPart" >{breadcrumbs.firstPartOfThePath}</a>
  }
  <span class="breadcrumbs__last">{breadcrumbs.lastPartOfThePath}</span>
</div>

<style>
  .breadcrumbs {
    font-size: 0.875em;
  }
  .breadcrumbs__first {
    font-weight: 900;
    font-size: 1.3em;
  }
  .breadcrumbs__last {
    font-size: 0.875em;
  }
</style>
